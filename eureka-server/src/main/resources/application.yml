server:
  port: 8761

eureka:
  client:
    register-with-eureka: false     #是否将eureka服务自身作为应用注册到eureka注册中心
    fetch-registry: false            #为true时,可以启动,但报异常：Cannot execute request on any known server